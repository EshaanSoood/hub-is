/**
 * Phase 1 rename reset:
 * pilot data is disposable, so the renamed database is created directly from
 * schema.mjs instead of incrementally migrating the old project/project schema.
 */
export const runMigrations = () => {
  // Intentionally empty.
};
